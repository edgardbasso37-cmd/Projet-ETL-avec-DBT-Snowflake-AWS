[project]
name = "projet-airbnb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Edgard BASSO", email = "edgardbasso37@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "dbt-core>=1.12.5",
    "dbt-snowflake>=1.12.1",
]

[project.scripts]
projet-airbnb = "projet_airbnb:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
